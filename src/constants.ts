export namespace constants {
    export const injectable = Symbol('injecty:injectable');
    export const properties = Symbol('injecty:properties');
    export const instance = Symbol('injecty:instance');
}